subscription GraphSubscription {
  graph {
    id
    expression
    nodes {
      id
      labels
      properties
    }
    edges {
      id
      type
      startNode
      endNode
      properties
    }
  }
}
